import styled from 'styled-components';
import { BG_PRIMARY_DARK } from 'style-constants';

export default styled.div`
  position: absolute;
  top: 0;
  left: 0;
  width: inherit;
  height: inherit;
  z-index: 3;
  background: ${BG_PRIMARY_DARK}D9;
`;
