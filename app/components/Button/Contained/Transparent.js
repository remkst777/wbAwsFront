import { css } from 'styled-components';
import { BG_TRANSPARENT, TEXT_DARK } from 'style-constants';

import BStyles from '../index';

const T = css`
  ${BStyles};
  background: ${BG_TRANSPARENT};
  color: ${TEXT_DARK};
`;

export default T;
