import { css } from 'styled-components';
import { BG_INFO, TEXT_LIGHT } from 'style-constants';

import BStyles from '../index';

const I = css`
  ${BStyles};
  background: ${BG_INFO};
  color: ${TEXT_LIGHT};
`;

export default I;
