import styled from 'styled-components';
import { BG_SUCCESS, TEXT_LIGHT } from 'style-constants';

export default styled.input`
  background: ${BG_SUCCESS}90;
  color: ${TEXT_LIGHT};
  padding: 15px 18px;
  font-size: 18px;
  font-weight: 300;
  outline: none;
  border: none;
  border-radius: 4px;
`;
