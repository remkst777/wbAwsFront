const scope = 'app.containers.HomePage';

export default {
  title: {
    id: `${scope}.title`,
  },
  description: {
    id: `${scope}.description`,
  },
  getEmail: {
    id: `${scope}.getEmail`,
  },
  when: {
    id: `${scope}.when`,
  },
  costLlBeReduced: {
    id: `${scope}.costLlBeReduced`,
  },
};
