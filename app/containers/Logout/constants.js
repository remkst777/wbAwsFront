/*
 *
 * Login constants
 *
 */

const key = 'containers.Logout';

const LOGOUT = `${key}.LOGOUT`;
const LOGOUT_SUCCESS = `${key}.LOGOUT_SUCCESS`;
const LOGOUT_ERROR = `${key}.LOGOUT_ERROR`;

export { LOGOUT, LOGOUT_SUCCESS, LOGOUT_ERROR };
